/**
 * Bit lives in Mascot.tsx now, alongside the rest of the cast. This file stays
 * so that `@/mascot/Bit` keeps working everywhere it is already imported.
 */
export { Bit, Mascot, CAST, CAST_LIST, speciesFor } from './Mascot';
export type { BitMood, Species, SpeciesId } from './Mascot';
